 = добавить енам типов вью, имплементит IViewType
ex:
    enum class ViewType : IViewType{
        FIRST,
        SECOND;

        override fun getType(): Int = ordinal
    }

 = добавить свой интерфейс (ака ListItem) метод которого возвращает вариант из енама, для удобства автоимпорта
 interface ListItem : IListItem {
     override fun getItemType(): IViewType {
         return getViewType()
     }

     fun getViewType(): ViewType
 }



 = добавить свой адаптер, возвращающий объект имплементирующий IFindViewType(ищет IViewType по инту),
    можно использовать ViewTypeFinder(), в конструктор передавать массив элементов енама, реализующего IViewType
ex:
    abstract class ItemAdapter : BaseItemAdapter(){

        override fun getViewTypeFinder(): IFindViewType = ViewTypeFinder(ViewType.values())
    }